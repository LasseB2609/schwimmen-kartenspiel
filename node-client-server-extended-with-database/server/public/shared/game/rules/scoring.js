//berechnung der Punktzahl => 31, 30.5, Feuer, normale Zahlenwerte

//keine Klasse hier, da das nur sinnvoll ist, wenn ein Zustand vorhanden ist -> allerdings haben die Punktwerte keinen Zustand(-wechsel) oder Verhalten


//Funktion, die den Wert der übergebenen Hand berechnet
function getScoreOfHand(hand) {
    let score = 0;

    for(const card of hand) {
        
    }

    return score;
}